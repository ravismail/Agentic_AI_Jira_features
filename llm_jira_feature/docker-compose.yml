version: "3.8"
services:
  app:
    build: .
    container_name: agentic-jira-app
    ports:
      - "8501:8501"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
